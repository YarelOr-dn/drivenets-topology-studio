# AI Protocol Topology Blueprint Library

Auto-generated catalog of the canonical protocol-topology blueprints the AI
assistant consults before emitting `create_topology`. Each entry uses the
per-protocol color coding, AS / area grouping shapes, and text-box annotations
documented in `topology/ai/knowledge.md`.

## Workflow

1. Assistant calls `list_blueprints({protocol, scale, tags, query})` to
   discover which blueprints match the user ask.
2. Assistant calls `load_blueprint({name})` to fetch the full JSON.
3. Assistant ADAPTS device counts / names / IPs to the user's specific
   scenario, then calls `create_topology` with the adapted objects.

Do NOT edit this file by hand -- regenerate via
`python3 ai/blueprints/_generate_index.py` from the `topology/` directory.

Per-user overrides live at `~/.topology_users/<user>/ai_blueprints/*.json`
and take precedence on name collision.

## Catalog

| Blueprint | Protocol | Scale | Devices | Links | Summary |
|---|---|---|---|---|---|
| `5g-xhaul-csr` | `5g` | medium | 10 | 11 | 5G xHaul topology with disaggregated O-RAN: RU <-fronthaul (eCPRI)-> DU <-midhaul (F1)-> CU <-backhaul-> 5G Core (UPF). IP/MPLS cell-site router aggregates multiple RUs, enforces strict timing (PTP G.8275.1) and per-slice QoS. |
| `bgp-confederation` | `bgp` | medium | 6 | 8 | BGP confederation: AS 65000 split into two private sub-ASes (65501 / 65502) to scale iBGP. Full-mesh iBGP inside each sub-AS, confederation eBGP between them. |
| `bng-pppoe-subscriber` | `bng` | medium | 8 | 8 | Broadband aggregation: residential CPEs dial PPPoE sessions over an access network to a BNG (Broadband Network Gateway). RADIUS authenticates subscribers and returns per-session QoS/ACL. BNG terminates PPP, places subscriber into VRF INET,  |
| `3tier-mlag` | `campus` | medium | 8 | 14 | Enterprise 3-tier campus (Core / Distribution / Access) with MLAG. OSPF between core and distribution; access uplinks as LAGs terminating on the MLAG peer-group. |
| `3stage-2x4` | `clos` | medium | 6 | 8 | Clos 3-stage DC fabric with 2 spines and 4 leaves. Canonical non-blocking spine-leaf design used by every modern hyperscale DC. |
| `5stage-super-spine` | `clos` | large | 16 | 31 | Clos 5-stage hyperscale DC fabric. Three super-spines aggregate five spines over eight leaves -- the scaling step beyond the 3-stage baseline. |
| `l2-extension-evpn-wan` | `dci` | medium | 6 | 5 | Data-Center Interconnect with L2 extension via EVPN-VXLAN over a shared WAN. Each DC runs its own fabric and stitches tenant VNIs end-to-end at the DCI gateways. |
| `dnaas-fabric` | `drivenets` | large | 10 | 12 | DriveNets DNAAS service fabric. Customer PEs front-end a leaf/spine/fabric disaggregated cluster for VPN + VPWS service delivery at scale. |
| `ncp-ncf-cluster` | `drivenets` | medium | 7 | 14 | DriveNets Network Cloud disaggregated cluster: 4 NCP packet boxes, 2 NCF fabric chassis, and NCM control plane. Canonical scale-out DNOS chassis replacement. |
| `ebgp-2as-transit` | `ebgp` | medium | 6 | 5 | eBGP transit between three ASes. AS 65002 transits routes between AS 65001 and AS 65003; iBGP inside each AS, eBGP at the boundaries. |
| `ixp-route-server` | `ebgp` | medium | 7 | 6 | IXP route-server topology. Each member AS peers with a single central route-server instead of building a full mesh, scaling peering from N^2 to N sessions. |
| `2spine-4leaf-anycast-gw` | `evpn-vxlan` | medium | 6 | 16 | EVPN-VXLAN DC fabric: 2 spines + 4 leaves with anycast-gateway IRB. eBGP underlay (Juniper / Arista / Cisco style), BGP-EVPN overlay. |
| `edge-routed-bridging` | `evpn-vxlan` | medium | 7 | 10 | EVPN-VXLAN Edge-Routed Bridging. Border leaves terminate L3 VNIs and peer to the WAN edge; server leaves stay pure L2. Modern DC alternative to centralised IRB. |
| `multi-tenant` | `evpn-vxlan` | medium | 9 | 10 | Multi-tenant EVPN-VXLAN. Two tenants isolated by VNI / VRF, sharing the same leaf-spine fabric. Demonstrates RT-import/export per tenant. |
| `bgp-flowspec-rtbh` | `flowspec` | medium | 6 | 6 | BGP FlowSpec (RFC 8955) + RTBH (RFC 5635/7999) combo for DDoS mitigation. Scrubbing controller originates FlowSpec rules (5-tuple match + action rate-limit/redirect) and RTBH /32 prefixes tagged with community 65535:666. ASBRs install them  |
| `vrrp-dual-gateway-bfd` | `ha` | small | 7 | 8 | Dual-router VRRP with BFD-accelerated failover. Two gateways share a VIP; Master (priority 110) handles traffic, Backup (priority 90) takes over within <1s on BFD DOWN. Typical enterprise/edge HA pattern. Interface tracking demotes priority |
| `ibgp-full-mesh-4` | `ibgp` | small | 4 | 6 | Classic 4-node iBGP full mesh. Every router peers with every other; session count scales as n(n-1)/2 -- the reason Route Reflectors exist. |
| `ibgp-rr-hub-spoke-6` | `ibgp` | medium | 6 | 9 | iBGP with a redundant Route-Reflector pair. Each PE peers with both RRs (N sessions per client) and the RRs peer with each other; the classic scaling fix to the N^2 iBGP full-mesh problem. |
| `igmp-snooping-l2` | `igmp` | small | 6 | 5 | Layer-2 IGMP snooping on an access switch. Without snooping the switch floods multicast to every port; with snooping it listens to IGMP Joins/Leaves and only forwards to interested ports. Upstream mrouter port discovered via IGMP queries. |
| `l1-l2-hierarchy` | `isis` | medium | 7 | 7 | IS-IS hierarchical design. Area 49.0001 (L1) attaches to the L2 backbone via two L1L2 routers. Classic SP backbone topology. |
| `pure-l2-backbone` | `isis` | medium | 8 | 11 | IS-IS pure-L2 backbone. All routers run level-2-only in a single area -- the scale-out IGP choice most SP core networks pick. |
| `mldp-p2mp-tree` | `mldp` | medium | 10 | 10 | Multicast LDP (mLDP) P2MP LSP tree. Root PE (ingress) signals a Point-to-Multipoint LSP via FEC advertisement; P routers replicate; leaf PEs terminate. Used as the transport tunnel for MVPN or IPTV multicast in MPLS networks. No PIM in the  |
| `2pe-1ce-basic` | `mpls-l3vpn` | small | 6 | 7 | Basic MPLS L3VPN: 2 PEs, 2 P routers in the core, 1 CE per site. LDP transport label + MP-BGP VPN label two-label forwarding. |
| `4pe-rr-hub` | `mpls-l3vpn` | medium | 7 | 11 | 4-PE MPLS L3VPN with a VPNv4 Route-Reflector. Each PE peers only with the RR (not a full mesh) for MP-BGP VPN distribution. |
| `multi-site-vpn` | `mpls-l3vpn` | medium | 9 | 12 | Multi-site MPLS L3VPN hub-and-spoke. Three spoke sites share routes via a central hub PE using asymmetric RT import/export (classic enterprise managed-services VPN design). |
| `mvpn-mpls-2pe` | `mvpn` | medium | 7 | 7 | NG-MVPN (RFC 6513/6514) over MPLS L3VPN. Two PEs, one P core router, customer multicast encapsulated in PMSI tunnels signaled by BGP MCAST-VPN AFI. C-sources/C-receivers on CEs in VRF CUST-A. Combines MPLS L3VPN blueprint with PIM inside th |
| `abr-dr-hierarchy` | `ospf` | medium | 9 | 11 | Full OSPF hierarchy: DR / BDR / DR-Other on a broadcast backbone plus two ABRs fan-out into Area 1 and Area 2. |
| `multi-area-0-1-2` | `ospf` | medium | 9 | 10 | OSPF multi-area: Area 0 backbone with two ABRs fan-out to Area 1 and Area 2. Canonical LSDB-scaling design. |
| `single-area-5` | `ospf` | small | 5 | 7 | OSPF single-area (Area 0) baseline. Five routers all speaking OSPF in the backbone area -- the starting point before partitioning into multi-area. |
| `totally-stubby` | `ospf` | small | 6 | 7 | OSPF totally stubby area. The ABR blocks both Type-3 and Type-5 LSAs into the stub; internal routers rely on a single 0.0.0.0/0 default. |
| `pim-sm-rp-4router` | `pim` | small | 7 | 8 | PIM Sparse-Mode (ASM) with a single Rendezvous Point. Source registers with the RP, receivers join shared tree (*,G), SPT switchover happens at the last-hop router. Classic Any-Source Multicast deployment inside one OSPF area. |
| `pim-ssm-source-specific` | `pim` | small | 6 | 6 | PIM Source-Specific Multicast (PIM-SSM, 232.0.0.0/8). Receivers explicitly join (S,G) via IGMPv3 -- no RP, no shared tree. Ideal for IPTV and one-to-many streaming where receivers know the source. |
| `qos-diffserv-pe` | `qos` | small | 5 | 4 | DiffServ QoS on a provider edge: customer traffic classified into EF (voice), AF41 (video), AF31 (business), BE (internet). Ingress policer caps per class, egress shaper enforces CIR, WRED drops BE early to protect priority classes. Canonic |
| `erps-ring` | `ring` | medium | 8 | 8 | 8-node ERPS (Ethernet Ring Protection Switching) ring with two core-facing gateways. Single RPL blocked during normal operation per G.8032v2. |
| `metro-g8032-6node` | `ring` | small | 6 | 6 | G.8032 metro Ethernet ring with 6 access nodes. One link is the Ring Protection Link (normally blocked). Sub-50 ms protection on failure. |
| `rsvp-te-frr-bypass` | `rsvp-te` | medium | 6 | 6 | RSVP-TE primary LSP from PE-A to PE-B with a Fast-Reroute bypass tunnel around mid-P router P2 (node-protection). On P2 failure, P1 switches the traffic onto the bypass in <50ms while head-end re-signals. Classic carrier-grade TE resiliency |
| `sr-mpls-ti-lfa` | `sr` | medium | 8 | 11 | SR-MPLS core with TI-LFA fast-reroute. Node-SIDs advertised via IS-IS SR; every router pre-computes a backup path for sub-50 ms convergence on link failure. |
| `sr-te-policy` | `sr` | medium | 8 | 14 | SR-TE policy-based traffic steering. A low-latency policy pushes an explicit SID-list that routes traffic along the southern path, overriding the IGP best-metric decision. |
| `rstp-access-3switch` | `stp` | small | 5 | 5 | RSTP (802.1w) triangle of 3 access switches. Root bridge elected (lowest priority), one port on each non-root switch becomes root port (lowest root-path-cost), remaining redundant link is put in BLOCKING to prevent the loop. Convergence <1s |
| `vpls-full-mesh` | `vpls` | small | 6 | 6 | VPLS (Virtual Private LAN Service) with full mesh of pseudowires between 3 PEs. All attached customer sites appear on ONE bridged L2 segment; BUM traffic replicated via the PW mesh. LDP signalling (RFC 4762) or BGP auto-discovery (RFC 4761) |
| `ipsec-hub-spoke` | `vpn` | small | 8 | 7 | IPsec site-to-site VPN in hub-and-spoke. Central HQ hub terminates IKEv2/ESP tunnels from 3 branch spokes over the public Internet. Spokes reach each other via the hub (phase-1). Classic pre-DMVPN enterprise WAN; pairs with BGP over the tun |
| `vpws-p2p` | `vpws` | small | 5 | 5 | VPWS (Virtual Private Wire Service) point-to-point pseudowire between 2 PEs. Delivers a transparent Ethernet EPL or EVPL to the customer -- whatever enters one AC exits the other. The simplest L2VPN: no MAC learning, no BUM replication. Bil |

## Tag cloud

- `mpls` (8)
- `ospf` (7)
- `multicast` (5)
- `ibgp` (4)
- `access` (4)
- `evpn` (4)
- `vxlan` (4)
- `l2` (4)
- `l3vpn` (4)
- `ebgp` (3)
- `vrf` (3)
- `hub-spoke` (3)
- `pim` (3)
- `abr` (3)
- `carrier` (2)
- `clos` (2)
- `spine-leaf` (2)
- `wan` (2)
- `drivenets` (2)
- `bgp` (2)
- `route-reflector` (2)
- `igmp` (2)
- `igmpv3` (2)
- `isis` (2)
- `8-node` (2)
- `lsp` (2)
- `rd-rt` (2)
- `multi-area` (2)
- `area-0` (2)
- `ring` (2)
- `erps` (2)
- `g8032` (2)
- `metro` (2)
- `fast-reroute` (2)
- `sr` (2)
- `l2vpn` (2)
- `pseudowire` (2)
- `pw` (2)
- `5g` (1)
- `mobile` (1)
- `xhaul` (1)
- `fronthaul` (1)
- `midhaul` (1)
- `backhaul` (1)
- `ecpri` (1)
- `ru` (1)
- `du` (1)
- `cu` (1)
- `upf` (1)
- `csr` (1)
- `confederation` (1)
- `scaling` (1)
- `bng` (1)
- `broadband` (1)
- `pppoe` (1)
- `ppp` (1)
- `subscriber` (1)
- `radius` (1)
- `aaa` (1)
- `l2tp-lac-lns` (1)
- `campus` (1)
- `3-tier` (1)
- `mlag` (1)
- `3-stage` (1)
- `dc-fabric` (1)
- `5-stage` (1)
- `super-spine` (1)
- `hyperscale` (1)
- `dci` (1)
- `l2-extension` (1)
- `dnaas` (1)
- `service-fabric` (1)
- `pe-ce` (1)
- `leaf-spine` (1)
- `ncp` (1)
- `ncf` (1)
- `ncm` (1)
- `cluster` (1)
- `dnos` (1)
- `transit` (1)
- `3-as` (1)
- `asbr` (1)
- `ixp` (1)
- `route-server` (1)
- `peering` (1)
- `anycast-gw` (1)
- `clos-3-stage` (1)
- `erb` (1)
- `border-leaf` (1)
- `irb` (1)
- `multi-tenant` (1)
- `vni` (1)
- `security` (1)
- `flowspec` (1)
- `rtbh` (1)
- `ddos` (1)
- `mitigation` (1)
- `blackhole` (1)
- `controller` (1)
- `rfc8955` (1)
- `rfc7999` (1)
- `ha` (1)
- `vrrp` (1)
- `bfd` (1)
- `redundancy` (1)
- `failover` (1)
- `gateway-redundancy` (1)
- `active-standby` (1)
- `interface-tracking` (1)
- `full-mesh` (1)
- `4-node` (1)
- `rr` (1)
- `6-node` (1)
- `snooping` (1)
- `bum` (1)
- `querier` (1)
- `l1` (1)
- `hierarchical` (1)
- `sp-backbone` (1)
- `pure-l2` (1)
- `sp-core` (1)
- `mldp` (1)
- `p2mp` (1)
- `ldp` (1)
- `tree` (1)
- `rfc6388` (1)
- `mvpn-transport` (1)
- `2pe` (1)
- `vpnv4` (1)
- `4pe` (1)
- `multi-site` (1)
- `rt-asymmetric` (1)
- `mvpn` (1)
- `ng-mvpn` (1)
- `mpls-l3vpn` (1)
- `pmsi` (1)
- `rosen` (1)
- `dr` (1)
- `bdr` (1)
- `area-1` (1)
- `area-2` (1)
- `single-area` (1)
- `5-node` (1)
- `totally-stubby` (1)
- `area-10` (1)
- `default-route` (1)
- `pim-sm` (1)
- `asm` (1)
- `rp` (1)
- `shared-tree` (1)
- `spt-switchover` (1)
- `pim-ssm` (1)
- `ssm` (1)
- `source-specific` (1)
- `iptv` (1)
- `no-rp` (1)
- `qos` (1)
- `cos` (1)
- `dscp` (1)
- `diffserv` (1)
- `policing` (1)
- `shaping` (1)
- `wred` (1)
- `ef` (1)
- `af41` (1)
- `af31` (1)
- `be` (1)
- `pe` (1)
- `rpl` (1)
- `rsvp-te` (1)
- `te` (1)
- `frr` (1)
- `bypass` (1)
- `node-protection` (1)
- `sr-mpls` (1)
- `ti-lfa` (1)
- `node-sid` (1)
- `sr-te` (1)
- `traffic-engineering` (1)
- `binding-sid` (1)
- `policy` (1)
- `stp` (1)
- `rstp` (1)
- `spanning-tree` (1)
- `loop-prevention` (1)
- `root-bridge` (1)
- `bpdu` (1)
- `vpls` (1)
- `elan` (1)
- `e-lan` (1)
- `meshed-pws` (1)
- `bum-replication` (1)
- `vpn` (1)
- `ipsec` (1)
- `ikev2` (1)
- `esp` (1)
- `site-to-site` (1)
- `encryption` (1)
- `tunnel` (1)
- `vpws` (1)
- `epl` (1)
- `evpl` (1)
- `e-line` (1)
- `p2p` (1)

_42 blueprints._
