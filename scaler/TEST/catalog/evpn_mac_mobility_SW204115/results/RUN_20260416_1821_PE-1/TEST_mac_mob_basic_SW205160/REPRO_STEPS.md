# Manual Reproduction Steps

**Device:** PE-1
**Generated:** 2026-04-16T18:25:09+00:00

---

## SC01_learn_local_ac: Learn MAC from local AC; expect Type-2 advertised

**Result:** FAIL

### Step 1: Verify Prerequisites

- Check: `bgp_l2vpn_evpn_established` -- Fix via: `Manual BGP L2VPN EVPN`
- Check: `evpn_instance_exists` -- Fix via: `config_generator + validate_config`
- Check: `traffic_or_spirent` -- Fix via: `/SPIRENT or DNAAS path`

### Step 2: Capture Baseline (Before Trigger)

Run these commands and save output for comparison:

```
PE-1# show evpn mac-table instance {evpn_name} | no-more
```

```
PE-1# show bgp l2vpn evpn summary | no-more
```

### Step 3: Trigger

**Action:** `traffic_on_ac1` (method: `spirent_or_manual`)

### Step 5: Verify

Run these verification commands:

```
PE-1# show evpn mac-table instance {evpn_name} mac {test_mac} | no-more
```

**What to check in the output:**

- [ ] Flags ABSENT: F, D
- [ ] Source is one of: ['local', 'ac']
- [ ] BGP Type-2 route exists for this MAC (`show bgp l2vpn evpn route mac-address <MAC>`)
- [ ] Forwarding state: forwarding
- [ ] No ghost MAC entries for this MAC

### Findings (automated test)

| Layer | Status | Detail |
|-------|--------|--------|
| preflight_smoke_test | FAIL | L2 path BROKEN: MAC 00:DE:AD:FF:FF:01 never appeared in HA_TEST_ELAN mac-table after 10.0s. Check: DNAAS BD, DUT AC stat |


## SC02_learn_remote_evpn: Learn MAC from remote EVPN RT-2

**Result:** FAIL

### Step 1: Verify Prerequisites

- Check: `bgp_l2vpn_evpn_established` -- Fix via: `Manual BGP L2VPN EVPN`
- Check: `evpn_instance_exists` -- Fix via: `config_generator + validate_config`
- Check: `traffic_or_spirent` -- Fix via: `/SPIRENT or DNAAS path`

### Step 2: Capture Baseline (Before Trigger)

Run these commands and save output for comparison:

```
PE-1# show evpn mac-table instance {evpn_name} | no-more
```

### Step 3: Trigger

**Action:** `remote_pe_traffic` (method: ``)

### Step 5: Verify

Run these verification commands:

```
PE-1# show evpn mac-table instance {evpn_name} | no-more
```

**What to check in the output:**

- [ ] Flags ABSENT: F, D
- [ ] Source is one of: ['bgp', 'evpn', 'remote']
- [ ] Forwarding state: forwarding
- [ ] No ghost MAC entries for this MAC

### Findings (automated test)

| Layer | Status | Detail |
|-------|--------|--------|
| preflight_smoke_test | FAIL | L2 path BROKEN: MAC 00:DE:AD:FF:FF:01 never appeared in HA_TEST_ELAN mac-table after 10.0s. Check: DNAAS BD, DUT AC stat |


## SC03_learn_vpls_pw: Learn MAC from VPLS PW (dynamic)

**Result:** FAIL

### Step 1: Verify Prerequisites

- Check: `bgp_l2vpn_evpn_established` -- Fix via: `Manual BGP L2VPN EVPN`
- Check: `evpn_instance_exists` -- Fix via: `config_generator + validate_config`
- Check: `traffic_or_spirent` -- Fix via: `/SPIRENT or DNAAS path`

### Step 2: Capture Baseline (Before Trigger)

Run these commands and save output for comparison:

```
PE-1# show evpn mac-table instance {evpn_name} | no-more
```

### Step 3: Trigger

**Action:** `traffic_via_pw` (method: `spirent_create_vpls_stream`)

### Step 5: Verify

Run these verification commands:

```
PE-1# show evpn mac-table instance {evpn_name} | no-more
```

**What to check in the output:**

- [ ] Flags ABSENT: F, D
- [ ] Source is one of: ['pw', 'pseudo', 'vpls']
- [ ] Forwarding state: forwarding
- [ ] No ghost MAC entries for this MAC

### Findings (automated test)

| Layer | Status | Detail |
|-------|--------|--------|
| preflight_smoke_test | FAIL | L2 path BROKEN: MAC 00:DE:AD:FF:FF:01 never appeared in HA_TEST_ELAN mac-table after 10.0s. Check: DNAAS BD, DUT AC stat |


## SC04_table_counts: Verify MAC table counts and forwarding continuity

**Result:** FAIL

### Step 1: Verify Prerequisites

- Check: `bgp_l2vpn_evpn_established` -- Fix via: `Manual BGP L2VPN EVPN`
- Check: `evpn_instance_exists` -- Fix via: `config_generator + validate_config`
- Check: `traffic_or_spirent` -- Fix via: `/SPIRENT or DNAAS path`

### Step 2: Capture Baseline (Before Trigger)

Run these commands and save output for comparison:

```
PE-1# show evpn mac-table instance {evpn_name} | no-more
```

### Step 5: Verify

Run these verification commands:

```
PE-1# show evpn summary | no-more
```

```
PE-1# show bridge-domain instance | no-more
```

**What to check in the output:**

- [ ] Flags ABSENT: F, D
- [ ] Forwarding state: forwarding
- [ ] No ghost MAC entries for this MAC

### Findings (automated test)

| Layer | Status | Detail |
|-------|--------|--------|
| preflight_smoke_test | FAIL | L2 path BROKEN: MAC 00:DE:AD:FF:FF:01 never appeared in HA_TEST_ELAN mac-table after 10.0s. Check: DNAAS BD, DUT AC stat |

---

_Generated by /TEST MAC Mobility Orchestrator_