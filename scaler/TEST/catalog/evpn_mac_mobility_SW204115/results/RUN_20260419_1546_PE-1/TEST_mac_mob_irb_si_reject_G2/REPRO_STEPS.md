# Manual Reproduction Steps

**Device:** PE-1
**Generated:** 2026-04-19T15:47:12+00:00

---

## SC01_add_irb_to_si_instance: router-interface (IRB) on SI-enabled instance -> commit check must REJECT

**Result:** FAIL

### Step 1: Verify Prerequisites

- Check: `evpn_instance_exists_with_si` -- Fix via: `operator`
- Check: `evpn_instance_exists_no_si` -- Fix via: `operator`

### Step 2: Capture Baseline (Before Trigger)

Run these commands and save output for comparison:

```
PE-1# show config network-services evpn instance {si_instance_name} | no-more
```

### Step 3: Trigger

**Action:** `attempt_irb_config` (method: `commit_check_assert`)

### Step 5: Verify

Run these verification commands:

```
PE-1# show config network-services evpn instance {si_instance_name} | no-more
```

**What to check in the output:**


### Findings (automated test)

| Layer | Status | Detail |
|-------|--------|--------|
| trigger | FAIL | commit check did NOT reject as expected. failed=False regex_hit=False output[:300]='NOTICE: commit action is not applica |
| bgp_session | WARN | 0/3 ESTABLISHED (not required for this test) |

---

_Generated by /TEST MAC Mobility Orchestrator_