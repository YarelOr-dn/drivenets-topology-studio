# Test plan requirements (canonical source)

The full DriveNets QA test item requirements document lives in the cheetah repo:

`cheetah/.ai/skills/qa/generate-qa-test-plan/test-documentation/test_plan_requirements.md`

Copy or symlink that file into `~/.cursor/tp-reference/test_plan_requirements.md` on machines that run TP generation, **or** rely on `tp_get_context` with `context_type=test_plan_requirements` which serves the bundled excerpt plus this pointer.

Confluence TP Checklist Guide page ID: **3934912829** (85 categories).
