# Portable /BGP

Cursor client for the lab ExaBGP host (in-band DNAAS). ExaBGP does **not** run here.

See [AGENT_QUICKSTART.md](AGENT_QUICKSTART.md) and `bash install-bgp.sh --dry-run --host <exabgp-host>`.
